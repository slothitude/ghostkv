"""GhostKV — Memory that haunts, not repeats.

An agent CLI that uses compressed KV memory as its persistent brain.
The agent's memory IS the KV cache. Not the token sequence. Not a vector DB.
"""

__version__ = "0.5.0"
